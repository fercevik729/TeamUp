# This tells lombok that this directory is the root and not to look anywhere
# else for java code
config.stopbubbling = true
# This adds the @lombok.Generated annotation to methods generated by
# lombok so they can be excluded from JaCoCo
lombok.addLombokGeneratedAnnotation = true